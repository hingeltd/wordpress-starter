{% extends "base.twig" %}

{% block content %}
    <main>
        <div class="columns">
            <div class="column">
                <h1>{{ title }}</h1>
                <p>{{ content|raw }}</p>
            </div>
        </div>
    </main>
{% endblock %}
